# merge v1.13.1 on the live Collie — trajectory, 2026-09-25

Explored after merging upstream v1.13.1 into the fork (commit b0d2fb71, live build `1.13.1-dev+8f218df3`).
Target: the operator's active Collie through the tailnet front door
the tailnet `https://<this machine>.<tailnet>.ts.net:8443` (asked of `tailscale status` by the config), scoped to a throwaway Herdr session with `?s=collie-e2e`.
Spec: `e2e-live/specs/merge-v1-13-1.spec.ts`. World: `session.sh up` / `session.sh down`.

## How to run

```bash
bash e2e-live/run.sh          # up, every spec, down (teardown runs even when a spec fails)
```

By hand: `bash e2e-live/session.sh up`, then `cd web && bunx playwright test -c ../e2e-live/playwright.config.ts`,
then `bash e2e-live/session.sh down`. Typecheck: `cd web && bunx tsc --noEmit -p ../e2e-live/tsconfig.json`.

## The world (`session.sh up`, about a minute)

- Named Herdr session `collie-e2e`, headless server. Collie fronts every running session
  (`multi_session` defaults on), so it appears as a second session with no config.
- `e2e-fixture`: a fresh `mktemp` git repo (`notes.md` committed then modified, `added.txt`
  untracked); tab `shell`, tab `agent` = plain `claude` parked on the folder-trust question (blocked).
- `e2e-agents` in `$HOME/.cache/collie-e2e/agents`: four `ccp-free` Claudes (free pool only):
  `ready-old` and `ready-new` each answer "ok" three seconds apart (done, unseen); `recent` answers
  and is then read once through Collie with `x-collie-seen` (seen); `working` runs `sleep 600`.
- Switcher, as asserted: Needs you (agent) → Ready · unseen (ready-new, ready-old) → Recent (recent)
  → Working (working).

## Environment traps and their answers

1. **Plain `127.0.0.1:8787` answers 403 `identity required`.** `COLLIE_TRUSTED_USER` is set, and the
   `Tailscale-User-Login` header only exists behind `tailscale serve`. Answer: baseURL is the tailnet
   URL (`playwright.config.ts`). The same holds for curl probes.
2. **A fresh browser context opens on the first-run screen**, a full-screen dialog ("Collie shows the
   agents in your terminal.") that intercepts every tap. Answer: seed `collie:tour:v1` =
   `TOUR_VERSION` before the first script runs (`fixtures.ts`, the `page` fixture; same technique as
   Tier 1's `seedTourSeen`).
3. **Open Browser Use drives a background tab: `document.visibilityState === "hidden"`.** The Changes
   tab only reads while visible (`use-workspace-change-counts.ts`, `useVisibleInterval`), so it sat on
   "Reading…" forever and sent no request. Not an app bug. Answer: exploration of anything
   visibility-gated moved to headless Playwright, where the page is visible. Bringing the Agent Chrome
   window to the front was rejected: it steals the operator's focus.
4. **Accessible names differ from the visible text.** The agent row's name includes the logo's alt
   (`claude logo agent … needs you`) and the shell tag is uppercase only in CSS (`shell shell`).
   `textContent` glues spans with no spaces (`agent在等你批准needs you`). Answer: match accessible
   names; read the description from the row's `ariaSnapshot()` first line.
5. **Headless browser is unpaired**: the page shows "Read-only — device not authorised". Harmless for
   these read-only cases; a case that sends keys would need a pairing credential first.
6. **Inside a Herdr pane, `HERDR_SOCKET_PATH` etc. point every `herdr` call at the operator's session.**
   Answer: `session.sh` drops the identity variables and passes `--session collie-e2e` on every call.
7. **Collie lists a removed session for a few seconds** after `session.sh down`, until its next
   discovery sweep. Expected (declared topology latency); gone on the next read.
8. **A server started from a Claude Code session inherits its `CLAUDE*` variables.** Every agent in
   the test session then saw a child-session marker (transcripts off, so no journal, so no
   description) and the parent's messaging token. Answer: `session.sh` starts the server with every
   `CLAUDE*`, `CODEX_COMPANION*` and `HERDR_*` variable removed.
9. **"Seen" is Collie's fact, not Herdr's.** `herdr agent focus` marked `recent` idle in Herdr, and
   Collie still filed it under Ready · unseen: unseen is `lastActiveAt > lastSeenAt` on Collie's side
   (`lib/triage.ts` `isUnseen`), and only a pane read carrying `x-collie-seen` moves `lastSeenAt`. A
   read made before Collie noticed the turn ending is overtaken by it. Answer: `collie_mark_seen`
   then `collie_seen` re-reads Collie's snapshot until the pane is settled and seen.
10. **The Working pane lasts ten minutes at most.** Claude's Bash tool caps a command there, then
    the pane turns `done` and every count moves (seen: Ready 3, Working 0). Answer: the `world` auto
    fixture checks the status counts before every case and fails with "the e2e world drifted"; run
    through `run.sh`, which rebuilds each time.
11. **Folders under the home directory skip Claude's trust question** (the home is trusted), which is
    why the four free agents live in `~/.cache/collie-e2e/agents` and the blocked one in `mktemp`.
12. **Text content drops spaces between spans** in section headings too: `Needs you(1)`. Answer: the
    heading regex allows `\s*` before the count.

## Successful trajectory (explored first in Open Browser Use, 390x844 mobile emulation)

| # | Action | Locator | Observed |
|---|---|---|---|
| 1 | open `/?s=collie-e2e` | — | heading `e2e-fixture`; rows `shell shell` and `claude logo agent 在等你批准 needs you`; footer `navigation "Dashboard views"` with `Panes` (aria-current=page), `Focus , 1 blocked`, `Changes` |
| 2 | tap the summary line (fork feature) | `main` › button `1 needs you Switch pane` | `dialog "Switch pane"` opens; section `NEEDS YOU (1)`; one row `claude logo agent 在等你批准`; no shell row |
| 3 | choose the agent in the switcher | dialog › button `claude logo agent <description>` | URL `/pane/w1%3Ap2?s=collie-e2e`; dialog gone; trust card drawn as buttons `❯ No, exit` / `↓ Yes, I trust this folder` (asserted, never tapped) |
| 4 | browser back (the iPhone edge swipe) | `page.goBack()` | URL `/?s=collie-e2e`; `Panes` aria-current=page |
| 5 | Focus tab | footer › button `/^\d* ?Focus/` | Focus aria-current; only the agent row; `New space` button gone (Launch and Spaces hidden); summary line still present |
| 6 | Changes tab | footer › button `Changes` | `list "Changes by workspace"` › button `e2e-fixture 2 files +2 −0` (in OBU it stayed "Reading…", trap 3) |
| 7 | tap the workspace row | list › button `e2e-fixture 2 files +2 −0` | URL `/space/w1/changes?s=collie-e2e`; h1 `Changes e2e-fixture`; buttons `Modified notes.md +1 −0`, `Untracked added.txt +1 −0`; `radiogroup "Layout"` List/Tree |
| 8 | in-app back arrow | button `Back to the dashboard` | URL `/?s=collie-e2e`; footer visible |

Cleanup: `session.sh down` stops and deletes the session, kills its server (and the Claude inside it),
removes the repo. OBU tabs finalized with keep `[]`.

## Where each answer lives

| Trap | Found in | Answer, by name |
|---|---|---|
| 1 identity 403 on loopback | round 1 | `frontDoor()` in `playwright.config.ts`; `front_door` in `session.sh` |
| 2 first-run dialog | round 1 | the `page` fixture in `fixtures.ts` (`TOUR_STORAGE_KEY`) |
| 3 OBU background tab | round 1 | none needed: exploration moved to headless Playwright |
| 4 accessible names vs text | round 1 | spec locators `agentRow`, `shellRow`, `agentDescription` (commented) |
| 5 unpaired, read-only | round 1 | none needed: every case reads only |
| 6 HERDR_* point at the operator | round 1 | `STRIP` and `h()` in `session.sh` |
| 7 removed session lingers | round 1 | none needed: expected sweep latency |
| 8 inherited CLAUDE_* | round 2 | `STRIP` in `session.sh` (server started with them removed) |
| 9 seen is Collie's fact | round 2 | `collie_mark_seen` + `collie_seen` in `session.sh` |
| 10 Working lasts 10 minutes | round 2 | the `world` auto fixture in `fixtures.ts`; `run.sh` rebuilds per run |
| 11 trust question under home | round 2 | `AGENTS_DIR` in `session.sh` |
| 12 no space before the count | round 2 | spec helper `section` (commented) |

## Round 2 (same day): switcher order

Explored in headless Playwright with `ariaSnapshot()` on `main` and on `dialog "Switch pane"`.
Summary line reads `1 needs you 2 unseen 1 working 1 done Switch pane`. Dialog headings (level 3):
`Needs you (1)`, `Ready · unseen (2)`, `Recent (1)`, `Working (1)`; rows `claude logo <tab label>
<description>`. Descriptions are model-written and differ per run, so cases pin the tab label only.
Focus keeps `agent`, `ready-old`, `ready-new`; drops `recent`, `working` and the shell.

## Not covered yet

- The words of a description (model-written, not stable). Only its presence and the fact that the
  switcher repeats the dashboard's words are pinned (for the blocked pane).
- Anything that sends keys: the headless browser is unpaired, so the page is read-only.
- Tablet viewport; WebKit.

## Appendix — run output (`bash e2e-live/run.sh`, round 2, world v2, after the audit fixes)

```
e2e-claude=blocked ready-old=done ready-new=done recent=done working=working 
up: session=collie-e2e fixture=w1 agents=w2 repo=$TMPDIR/collie-e2e-repo.XXXXXX
Running 6 tests using 1 worker
  ✓  1 [phone] › specs/merge-v1-13-1.spec.ts:57:3 › merge v1.13.1 on the live Collie › the dashboard shows the e2e world: one shell, one agent that needs you (408ms)
  ✓  2 [phone] › specs/merge-v1-13-1.spec.ts:65:3 › merge v1.13.1 on the live Collie › the summary opens the switcher, which lists agents only and repeats the description (273ms)
  ✓  3 [phone] › specs/merge-v1-13-1.spec.ts:73:3 › merge v1.13.1 on the live Collie › choosing from the switcher opens the pane, and back goes up to the dashboard (915ms)
  ✓  4 [phone] › specs/merge-v1-13-1.spec.ts:87:3 › merge v1.13.1 on the live Collie › the switcher orders its sections, and the latest state change leads inside one (ADR 0072) (268ms)
  ✓  5 [phone] › specs/merge-v1-13-1.spec.ts:107:3 › merge v1.13.1 on the live Collie › Focus keeps what needs you and what finished unseen, and drops the rest (251ms)
  ✓  6 [phone] › specs/merge-v1-13-1.spec.ts:121:3 › merge v1.13.1 on the live Collie › Changes counts the workspace, opens its files, and the back arrow returns home (748ms)
  6 passed (3.4s)
down: session collie-e2e removed
```
